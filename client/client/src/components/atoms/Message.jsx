import React from "react";
import { Typography } from "@mui/material";

const Message = () => (
  <Typography
    variant="h5"
    component="h2"
    sx={{ marginRight: "16px", marginBottom: "3rem" }}
  >
    Book Search Assignment
  </Typography>
);

export default Message;
